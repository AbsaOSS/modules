"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema SamplingRule:
    r"""
    SamplingRule is the Schema for the SamplingRules API. Creates and manages an AWS XRay Sampling Rule.

    Attributes
    ----------
    apiVersion : str, default is "xray.aws.m.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "SamplingRule", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : XrayAwsmUpboundIoV1beta1SamplingRuleSpec, default is Undefined, required
        spec
    status : XrayAwsmUpboundIoV1beta1SamplingRuleStatus, default is Undefined, optional
        status
    """


    apiVersion: "xray.aws.m.upbound.io/v1beta1" = "xray.aws.m.upbound.io/v1beta1"

    kind: "SamplingRule" = "SamplingRule"

    metadata?: v1.ObjectMeta

    spec: XrayAwsmUpboundIoV1beta1SamplingRuleSpec

    status?: XrayAwsmUpboundIoV1beta1SamplingRuleStatus


schema XrayAwsmUpboundIoV1beta1SamplingRuleSpec:
    r"""
    SamplingRuleSpec defines the desired state of SamplingRule

    Attributes
    ----------
    forProvider : XrayAwsmUpboundIoV1beta1SamplingRuleSpecForProvider, default is Undefined, required
        for provider
    initProvider : XrayAwsmUpboundIoV1beta1SamplingRuleSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : XrayAwsmUpboundIoV1beta1SamplingRuleSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : XrayAwsmUpboundIoV1beta1SamplingRuleSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: XrayAwsmUpboundIoV1beta1SamplingRuleSpecForProvider

    initProvider?: XrayAwsmUpboundIoV1beta1SamplingRuleSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: XrayAwsmUpboundIoV1beta1SamplingRuleSpecProviderConfigRef

    writeConnectionSecretToRef?: XrayAwsmUpboundIoV1beta1SamplingRuleSpecWriteConnectionSecretToRef


schema XrayAwsmUpboundIoV1beta1SamplingRuleSpecForProvider:
    r"""
    xray awsm upbound io v1beta1 sampling rule spec for provider

    Attributes
    ----------
    attributes : {str:str}, default is Undefined, optional
        Matches attributes derived from the request.
    fixedRate : float, default is Undefined, optional
        The percentage of matching requests to instrument, after the reservoir is exhausted.
    host : str, default is Undefined, optional
        Matches the hostname from a request URL.
    httpMethod : str, default is Undefined, optional
        Matches the HTTP method of a request.
    priority : float, default is Undefined, optional
        The priority of the sampling rule.
    region : str, default is Undefined, required
        Region where this resource will be managed. Defaults to the Region set in the provider configuration.
        Region is the region you'd like your resource to be created in.
    reservoirSize : float, default is Undefined, optional
        A fixed number of matching requests to instrument per second, prior to applying the fixed rate. The reservoir is not used directly by services, but applies to all services using the rule collectively.
    resourceArn : str, default is Undefined, optional
        Matches the ARN of the AWS resource on which the service runs.
    serviceName : str, default is Undefined, optional
        Matches the name that the service uses to identify itself in segments.
    serviceType : str, default is Undefined, optional
        Matches the origin that the service uses to identify its type in segments.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    urlPath : str, default is Undefined, optional
        Matches the path from a request URL.
    version : float, default is Undefined, optional
        The version of the sampling rule format (1 )
    """


    attributes?: {str:str}

    fixedRate?: float

    host?: str

    httpMethod?: str

    priority?: float

    region: str

    reservoirSize?: float

    resourceArn?: str

    serviceName?: str

    serviceType?: str

    tags?: {str:str}

    urlPath?: str

    version?: float


schema XrayAwsmUpboundIoV1beta1SamplingRuleSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    attributes : {str:str}, default is Undefined, optional
        Matches attributes derived from the request.
    fixedRate : float, default is Undefined, optional
        The percentage of matching requests to instrument, after the reservoir is exhausted.
    host : str, default is Undefined, optional
        Matches the hostname from a request URL.
    httpMethod : str, default is Undefined, optional
        Matches the HTTP method of a request.
    priority : float, default is Undefined, optional
        The priority of the sampling rule.
    reservoirSize : float, default is Undefined, optional
        A fixed number of matching requests to instrument per second, prior to applying the fixed rate. The reservoir is not used directly by services, but applies to all services using the rule collectively.
    resourceArn : str, default is Undefined, optional
        Matches the ARN of the AWS resource on which the service runs.
    serviceName : str, default is Undefined, optional
        Matches the name that the service uses to identify itself in segments.
    serviceType : str, default is Undefined, optional
        Matches the origin that the service uses to identify its type in segments.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    urlPath : str, default is Undefined, optional
        Matches the path from a request URL.
    version : float, default is Undefined, optional
        The version of the sampling rule format (1 )
    """


    attributes?: {str:str}

    fixedRate?: float

    host?: str

    httpMethod?: str

    priority?: float

    reservoirSize?: float

    resourceArn?: str

    serviceName?: str

    serviceType?: str

    tags?: {str:str}

    urlPath?: str

    version?: float


schema XrayAwsmUpboundIoV1beta1SamplingRuleSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema XrayAwsmUpboundIoV1beta1SamplingRuleSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema XrayAwsmUpboundIoV1beta1SamplingRuleStatus:
    r"""
    SamplingRuleStatus defines the observed state of SamplingRule.

    Attributes
    ----------
    atProvider : XrayAwsmUpboundIoV1beta1SamplingRuleStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [XrayAwsmUpboundIoV1beta1SamplingRuleStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: XrayAwsmUpboundIoV1beta1SamplingRuleStatusAtProvider

    conditions?: [XrayAwsmUpboundIoV1beta1SamplingRuleStatusConditionsItems0]

    observedGeneration?: int


schema XrayAwsmUpboundIoV1beta1SamplingRuleStatusAtProvider:
    r"""
    xray awsm upbound io v1beta1 sampling rule status at provider

    Attributes
    ----------
    arn : str, default is Undefined, optional
        The ARN of the sampling rule.
    attributes : {str:str}, default is Undefined, optional
        Matches attributes derived from the request.
    fixedRate : float, default is Undefined, optional
        The percentage of matching requests to instrument, after the reservoir is exhausted.
    host : str, default is Undefined, optional
        Matches the hostname from a request URL.
    httpMethod : str, default is Undefined, optional
        Matches the HTTP method of a request.
    id : str, default is Undefined, optional
        The name of the sampling rule.
    priority : float, default is Undefined, optional
        The priority of the sampling rule.
    region : str, default is Undefined, optional
        Region where this resource will be managed. Defaults to the Region set in the provider configuration.
        Region is the region you'd like your resource to be created in.
    reservoirSize : float, default is Undefined, optional
        A fixed number of matching requests to instrument per second, prior to applying the fixed rate. The reservoir is not used directly by services, but applies to all services using the rule collectively.
    resourceArn : str, default is Undefined, optional
        Matches the ARN of the AWS resource on which the service runs.
    serviceName : str, default is Undefined, optional
        Matches the name that the service uses to identify itself in segments.
    serviceType : str, default is Undefined, optional
        Matches the origin that the service uses to identify its type in segments.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    tagsAll : {str:str}, default is Undefined, optional
        A map of tags assigned to the resource, including those inherited from the provider default_tags configuration block.
    urlPath : str, default is Undefined, optional
        Matches the path from a request URL.
    version : float, default is Undefined, optional
        The version of the sampling rule format (1 )
    """


    arn?: str

    attributes?: {str:str}

    fixedRate?: float

    host?: str

    httpMethod?: str

    id?: str

    priority?: float

    region?: str

    reservoirSize?: float

    resourceArn?: str

    serviceName?: str

    serviceType?: str

    tags?: {str:str}

    tagsAll?: {str:str}

    urlPath?: str

    version?: float


schema XrayAwsmUpboundIoV1beta1SamplingRuleStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


