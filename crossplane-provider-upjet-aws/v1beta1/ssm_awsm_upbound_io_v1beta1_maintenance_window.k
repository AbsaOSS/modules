"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema MaintenanceWindow:
    r"""
    MaintenanceWindow is the Schema for the MaintenanceWindows API. Provides an SSM Maintenance Window resource

    Attributes
    ----------
    apiVersion : str, default is "ssm.aws.m.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "MaintenanceWindow", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : SsmAwsmUpboundIoV1beta1MaintenanceWindowSpec, default is Undefined, required
        spec
    status : SsmAwsmUpboundIoV1beta1MaintenanceWindowStatus, default is Undefined, optional
        status
    """


    apiVersion: "ssm.aws.m.upbound.io/v1beta1" = "ssm.aws.m.upbound.io/v1beta1"

    kind: "MaintenanceWindow" = "MaintenanceWindow"

    metadata?: v1.ObjectMeta

    spec: SsmAwsmUpboundIoV1beta1MaintenanceWindowSpec

    status?: SsmAwsmUpboundIoV1beta1MaintenanceWindowStatus


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowSpec:
    r"""
    MaintenanceWindowSpec defines the desired state of MaintenanceWindow

    Attributes
    ----------
    forProvider : SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecForProvider, default is Undefined, required
        for provider
    initProvider : SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecForProvider

    initProvider?: SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecProviderConfigRef

    writeConnectionSecretToRef?: SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecWriteConnectionSecretToRef


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecForProvider:
    r"""
    ssm awsm upbound io v1beta1 maintenance window spec for provider

    Attributes
    ----------
    allowUnassociatedTargets : bool, default is Undefined, optional
        Whether targets must be registered with the Maintenance Window before tasks can be defined for those targets.
    cutoff : float, default is Undefined, optional
        The number of hours before the end of the Maintenance Window that Systems Manager stops scheduling new tasks for execution.
    description : str, default is Undefined, optional
        A description for the maintenance window.
    duration : float, default is Undefined, optional
        The duration of the Maintenance Window in hours.
    enabled : bool, default is Undefined, optional
        Whether the maintenance window is enabled. Default: true.
    endDate : str, default is Undefined, optional
        Timestamp in ISO-8601 extended format when to no longer run the maintenance window.
    name : str, default is Undefined, optional
        The name of the maintenance window.
    region : str, default is Undefined, required
        Region where this resource will be managed. Defaults to the Region set in the provider configuration.
        Region is the region you'd like your resource to be created in.
    schedule : str, default is Undefined, optional
        The schedule of the Maintenance Window in the form of a cron or rate expression.
    scheduleOffset : float, default is Undefined, optional
        The number of days to wait after the date and time specified by a CRON expression before running the maintenance window. Valid range is 1 to 6.
    scheduleTimezone : str, default is Undefined, optional
        Timezone for schedule in Internet Assigned Numbers Authority (IANA) Time Zone Database format. For example: America/Los_Angeles, etc/UTC, or Asia/Seoul.
    startDate : str, default is Undefined, optional
        Timestamp in ISO-8601 extended format when to begin the maintenance window.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    """


    allowUnassociatedTargets?: bool

    cutoff?: float

    description?: str

    duration?: float

    enabled?: bool

    endDate?: str

    name?: str

    region: str

    schedule?: str

    scheduleOffset?: float

    scheduleTimezone?: str

    startDate?: str

    tags?: {str:str}


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    allowUnassociatedTargets : bool, default is Undefined, optional
        Whether targets must be registered with the Maintenance Window before tasks can be defined for those targets.
    cutoff : float, default is Undefined, optional
        The number of hours before the end of the Maintenance Window that Systems Manager stops scheduling new tasks for execution.
    description : str, default is Undefined, optional
        A description for the maintenance window.
    duration : float, default is Undefined, optional
        The duration of the Maintenance Window in hours.
    enabled : bool, default is Undefined, optional
        Whether the maintenance window is enabled. Default: true.
    endDate : str, default is Undefined, optional
        Timestamp in ISO-8601 extended format when to no longer run the maintenance window.
    name : str, default is Undefined, optional
        The name of the maintenance window.
    schedule : str, default is Undefined, optional
        The schedule of the Maintenance Window in the form of a cron or rate expression.
    scheduleOffset : float, default is Undefined, optional
        The number of days to wait after the date and time specified by a CRON expression before running the maintenance window. Valid range is 1 to 6.
    scheduleTimezone : str, default is Undefined, optional
        Timezone for schedule in Internet Assigned Numbers Authority (IANA) Time Zone Database format. For example: America/Los_Angeles, etc/UTC, or Asia/Seoul.
    startDate : str, default is Undefined, optional
        Timestamp in ISO-8601 extended format when to begin the maintenance window.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    """


    allowUnassociatedTargets?: bool

    cutoff?: float

    description?: str

    duration?: float

    enabled?: bool

    endDate?: str

    name?: str

    schedule?: str

    scheduleOffset?: float

    scheduleTimezone?: str

    startDate?: str

    tags?: {str:str}


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowStatus:
    r"""
    MaintenanceWindowStatus defines the observed state of MaintenanceWindow.

    Attributes
    ----------
    atProvider : SsmAwsmUpboundIoV1beta1MaintenanceWindowStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [SsmAwsmUpboundIoV1beta1MaintenanceWindowStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: SsmAwsmUpboundIoV1beta1MaintenanceWindowStatusAtProvider

    conditions?: [SsmAwsmUpboundIoV1beta1MaintenanceWindowStatusConditionsItems0]

    observedGeneration?: int


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowStatusAtProvider:
    r"""
    ssm awsm upbound io v1beta1 maintenance window status at provider

    Attributes
    ----------
    allowUnassociatedTargets : bool, default is Undefined, optional
        Whether targets must be registered with the Maintenance Window before tasks can be defined for those targets.
    cutoff : float, default is Undefined, optional
        The number of hours before the end of the Maintenance Window that Systems Manager stops scheduling new tasks for execution.
    description : str, default is Undefined, optional
        A description for the maintenance window.
    duration : float, default is Undefined, optional
        The duration of the Maintenance Window in hours.
    enabled : bool, default is Undefined, optional
        Whether the maintenance window is enabled. Default: true.
    endDate : str, default is Undefined, optional
        Timestamp in ISO-8601 extended format when to no longer run the maintenance window.
    id : str, default is Undefined, optional
        The ID of the maintenance window.
    name : str, default is Undefined, optional
        The name of the maintenance window.
    region : str, default is Undefined, optional
        Region where this resource will be managed. Defaults to the Region set in the provider configuration.
        Region is the region you'd like your resource to be created in.
    schedule : str, default is Undefined, optional
        The schedule of the Maintenance Window in the form of a cron or rate expression.
    scheduleOffset : float, default is Undefined, optional
        The number of days to wait after the date and time specified by a CRON expression before running the maintenance window. Valid range is 1 to 6.
    scheduleTimezone : str, default is Undefined, optional
        Timezone for schedule in Internet Assigned Numbers Authority (IANA) Time Zone Database format. For example: America/Los_Angeles, etc/UTC, or Asia/Seoul.
    startDate : str, default is Undefined, optional
        Timestamp in ISO-8601 extended format when to begin the maintenance window.
    tags : {str:str}, default is Undefined, optional
        Key-value map of resource tags.
    tagsAll : {str:str}, default is Undefined, optional
        A map of tags assigned to the resource, including those inherited from the provider default_tags configuration block.
    """


    allowUnassociatedTargets?: bool

    cutoff?: float

    description?: str

    duration?: float

    enabled?: bool

    endDate?: str

    id?: str

    name?: str

    region?: str

    schedule?: str

    scheduleOffset?: float

    scheduleTimezone?: str

    startDate?: str

    tags?: {str:str}

    tagsAll?: {str:str}


schema SsmAwsmUpboundIoV1beta1MaintenanceWindowStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


